cache:
  paths:
    - node_modules/

stages:
  - build
  - test
  - deploy

build:
  stage: build
  script:
    - npm install
    - npm run build
  artifacts:
    paths:
      - ./build
  only:
    - master

test:
  stage: test
  script:
    - echo "Running react test suite......"
    - npm test
  only:
    - master

deploy1:
  stage: deploy
  script:
    - cp -r ./build/* /home/ubuntu/kk

