before_script:
  - n=$(which node);n=${n%/bin/node}; chmod -R 755 $n/bin/*; sudo cp -r $n/{bin,lib,share} /usr/local
  - npm install -g n

stages:
  - build
  - test
  - deploy


build-job:   
  stage: build
  script:
    - npm install -g n
    - npm run build
    
 

